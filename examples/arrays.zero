// Array examples for Zero language

// Basic array with type annotation
let numbers: [int] = [1, 2, 3, 4, 5];
print(numbers);

// Array of strings
let names: [string] = ["Alice", "Bob", "Charlie"];
print(names);

// Empty array needs type annotation
let empty: [int] = [];
print(empty);

// Array type inference (if implemented)
let values = [10, 20, 30];
print(values);