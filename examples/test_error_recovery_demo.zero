// 这个文件包含多个语法错误，用于演示错误恢复机制

let x = 10;

// 错误1: 缺少分号
let y = 20

// 这个声明应该能被正确解析
let z = 30;

// 错误2: 缺少变量名
let = 40;

// 错误3: 函数缺少参数列表的右括号
fn test( {
  return 42;
}

// 这个函数应该能被正确解析
fn valid() {
  return 1;
}

// 错误4: 表达式不完整
let a = 10 + ;

// 最后一个正确的声明
let b = 50;
